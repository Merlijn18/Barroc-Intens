<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Inlog.InlogOverViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Inlog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="InlogPanel" Orientation="Vertical" VerticalAlignment="Center" Spacing="20" Grid.Row="0" Margin="0,130,0,10">
            <TextBlock x:Name="TitleText" Width="460" FontFamily="Forte" FontSize="80" HorizontalAlignment="Center"/>
            <TextBox x:Name="NameEmailTextBox" Width="200" PlaceholderText="Naam/email" />
            <PasswordBox x:Name="PasswordTextBox" Width="200" PlaceholderText="Password" />
            <Button x:Name="InlogButton" Content="Inloggen" Click="InlogButton_Click" HorizontalAlignment="Center"/>
            <TextBlock x:Name="MessageText" Foreground="Red" Grid.Row="3" HorizontalAlignment="Center" FontWeight="Bold" />
        </StackPanel>





        <ListView x:Name="inlogListView" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Padding="5">
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
