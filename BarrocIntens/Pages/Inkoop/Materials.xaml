<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Inkoop.Materials"
    xmlns:localdata="using:BarrocIntens.Models"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Inkoop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#f5f5f5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="Black" Height="100">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Inkoop" Foreground="#FDD716" FontSize="40" FontWeight="Bold" VerticalAlignment="Center" Margin="30,0,0,0"/>

            <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center" Spacing="10" Margin="20">
                <Button x:Name="Back" Content="Terug" Click="Back_Click" Background="Black" Foreground="White" BorderBrush="White" BorderThickness="1" Padding="12,6"/>
                <Button x:Name="Logout" Content="Uitloggen" Click="Logout_Click" Background="#FDD716" Foreground="Black" FontWeight="Bold" Padding="12,6"/>
            </StackPanel>
        </Grid>

        <RelativePanel Grid.Row="1" Margin="30,30,30,10">
            <StackPanel x:Name="SearchArea" Orientation="Horizontal" Spacing="15">
                <TextBlock Text="Search:" VerticalAlignment="Center" FontWeight="Bold" FontSize="18"/>
                <TextBox x:Name="SearchBox" Width="350" PlaceholderText="Zoeken..." VerticalAlignment="Center"/>
                <Button x:Name="AddMaterialButton" Content="Nieuw Materiaal" Background="#4CAF50" Foreground="White" Padding="20,10" CornerRadius="5" Click="AddMaterialButton_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="10" RelativePanel.AlignRightWithPanel="True">
                <Button x:Name="ShowMaterialsButton" Content="Materialenlijst Tonen" Background="#2196F3" Foreground="White" Padding="15,8" Click="ShowMaterialsButton_Click"/>
                <Button x:Name="HideMaterialsButton" Content="Verberg Lijst" Background="#F44336" Foreground="White" Padding="15,8" Visibility="Collapsed" Click="HideMaterialsButton_Click"/>
            </StackPanel>
        </RelativePanel>

        <ScrollViewer Grid.Row="2" Margin="30,10,30,30">
            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="25">
                <StackPanel>
                    <Grid Margin="0,0,0,20">
                        <TextBlock Text="Materialen Overzicht" FontSize="22" FontWeight="Bold" Foreground="#333"/>
                        <TextBlock x:Name="LowStockWarning" Text="Let op: Lage voorraad gedetecteerd!" Foreground="Red" FontWeight="Bold" HorizontalAlignment="Right" Visibility="Collapsed"/>
                    </Grid>

                    <ListView x:Name="ProductListView" Visibility="Collapsed" SelectionMode="None">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="4" HorizontalAlignment="Stretch"/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>

                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="localdata:Material">
                                <Border BorderBrush="#EEE" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,15,15" Background="#FAFAFA" Width="280">
                                    <StackPanel Spacing="8">
                                        <TextBlock Text="{x:Bind Productname}" FontWeight="Bold" FontSize="16" Foreground="Black"/>

                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock Text="Prijs:" Foreground="Gray"/>
                                            <TextBlock Text="{x:Bind PriceFormatted}" FontWeight="SemiBold"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock Text="Voorraad:" Foreground="Gray"/>
                                            <TextBlock Text="{x:Bind Stock}" FontWeight="SemiBold" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <TextBlock x:Name="PlaceholderText" Text="Klik op 'Materialenlijst Tonen' om de voorraad te bekijken." HorizontalAlignment="Center" Margin="0,50" Foreground="Gray" FontStyle="Italic"/>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Page>